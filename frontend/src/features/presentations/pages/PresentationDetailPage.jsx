import { PresentationDetailContainer } from '../containers/PresentationDetailContainer'

export default function PresentationDetailPage({ overrideId }) {
  return <PresentationDetailContainer overrideId={overrideId} />
}
